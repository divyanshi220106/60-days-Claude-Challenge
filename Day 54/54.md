# DAY4-SUMMARY.md — TaskFlow Capstone

## Status: Milestone 1 in progress (not yet verified)

## ✅ Implemented Today (Milestone 1 — Backend Authentication)
- `User` model with bcrypt password hashing
- `POST /api/auth/register` endpoint
- `POST /api/auth/login` endpoint
- `GET /api/auth/me` protected route
- JWT-based auth middleware
- MongoDB connection wired into `server.js`

## ⏳ Pending Your Verification
This milestone has **not been tested successfully yet**. A test attempt via the login.html signup form returned "Could not reach the server" — backend wasn't running/reachable at the time.

Before moving to Milestone 2, need to:
```bash
cd server
npm install jsonwebtoken bcryptjs
npm run dev
```
Confirm the terminal shows `MongoDB Connected` and `Server running on port 5000`, then re-test register/login and confirm a `200`/`201` response with a token.

## 🚧 Next (Milestone 2 — pending Milestone 1 verification)
- Frontend Signup/Login pages
- Auth context/state on the client
- Protected dashboard route
- Task CRUD API

## 🎯 Tomorrow's Objective (Day 5)
Task CRUD feature — create, view, update, delete tasks — connected to the authenticated user, once today's auth milestone(s) are confirmed working.
